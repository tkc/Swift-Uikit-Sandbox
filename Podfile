# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'Swift-Uikit-Sandbox' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Swift-Uikit-Sandbox
  
  pod 'SnapKit', '~> 0.15.0'
  pod 'BubbleTransition', '~> 1.0.0'
  pod 'FontAwesome.swift'

  target 'Swift-Uikit-SandboxTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Swift-Uikit-SandboxUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
